<!DOCTYPE html>
<html>
<head>
  <title>MovieBox</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= javascript_importmap_tags 'application' %>
</head>
<body>
  <% flash.each do |type, message| %>
    <div class="alert alert-info mb-0" id="flash-message">
        <%= message %>
    </div>
  <% end %>
  <header class="navbar navbar-dark bg-dark">
    <div class="container d-flex justify-content-between align-items-center p-0">
      <a class="navbar-brand" href=<%= root_path %>>
        <span style="font-size: 40px; font-weight: bold; color: mediumaquamarine">MovieBox</span>
      </a>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
            <%= link_to "Sign up", new_user_registration_path , class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </header>

  <main class="container mt-4 pb-5">
    <%= yield %>
  </main>

  <footer class="bg-dark text-white text-center py-1 mt-15 fixed-bottom">
    &copy; <%= Time.now.year %> MovieBox. All rights reserved.
  </footer>
  <%= javascript_include_tag 'review', 'data-turbolinks-track': 'reload' %>
</body>
</html>
