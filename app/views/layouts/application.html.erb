<!DOCTYPE html>
<html>
<head>
  <title>MovieBox</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <style>
    /* Set a minimum height for the main container to prevent the footer from moving */
    body {
      min-height: calc(100vh - 110px); /* Adjust as needed based on your footer height */
    }

    /* Style the navigation links */
    .navbar-nav li {
      margin-right: 10px;
      list-style: none;
      display: inline-block;
    }

    .navbar-nav li a {
      color: white;
      text-decoration: none;
      font-weight: bold;
    }

    .navbar-nav li a:hover {
      color: #f0f0f0;
    }
  </style>
</head>
<body>
  <header class="navbar navbar-dark bg-dark">
    <div class="container d-flex justify-content-between align-items-center">
      <a class="navbar-brand" href=<%= root_path %>>
        <span style="font-size: 40px; font-weight: bold; color: mediumaquamarine">MovieBox</span>
      </a>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
            <%= link_to "Sign up", user_registration_path , class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </header>

  <main class="container mt-4">
    <%= yield %>
  </main>

  <footer class="bg-dark text-white text-center py-3 mt-4">
    &copy; <%= Time.now.year %> MovieBox. All rights reserved.
  </footer>

  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</body>
</html>
